<%= form_for(@iso) do |f| %>
  <% if @iso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@iso.errors.count, "error") %> prohibited this iso from being saved:</h2>

      <ul>
      <% @iso.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :tage %><br>
    <%= f.text_field :tage %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
