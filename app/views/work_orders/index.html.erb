<h1>Listing Work Orders</h1>

<table  class='table drawing table-striped'>
  <thead>
    <tr>
      <th>Title</th>
      <th>Equipment</th>
      <th>Date</th>
    </tr>
  </thead>

  <tbody>
    <% @work_orders.each do |work_order| %>
      <tr>
        <td><%= work_order.title %></td>
        <td><%= work_order.equipment %></td>
        <td><%= work_order.date %></td>
      </tr>
    <% end %>
  </tbody>

  <div><%= link_to 'Add A Work Order', new_work_order_path, :class => "btn btn-primary dropdown-toggle" %></div>
  <div class='search-bar'>
    <form class="navbar-form navbar-left" role="search">
      <div class="form-group">
         <input type="text" class="form-control search-space" placeholder="Search">
         <button type="submit" class="btn btn-default search-submit">Search</button>
      </div>
    </form>
  </div>
     <%= will_paginate @work_orders, renderer: BootstrapPagination::Rails %>
</table>
